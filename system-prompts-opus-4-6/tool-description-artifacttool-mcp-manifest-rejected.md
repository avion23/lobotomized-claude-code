<!--
name: 'Tool Result: MCP Manifest Rejected'
description: >-
  ArtifactTool tool_result noting the MCP manifest was rejected and the page
  published without the connector bridge.
ccVersion: 2.1.178
variables:
  - TOOL_DESCRIPTION_ARTIFACTTOOL_MCP_MANIFEST_REJECTED_VAR_0
-->


⚠ The mcp manifest was rejected by the server and the page was published without it (the page's connector bridge will be unavailable). Server said: ${TOOL_DESCRIPTION_ARTIFACTTOOL_MCP_MANIFEST_REJECTED_VAR_0.mcpDropped}
